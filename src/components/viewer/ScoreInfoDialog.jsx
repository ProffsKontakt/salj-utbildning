// The viewer and the library share one "Redigera info" dialog so fields, copy and
// error messages stay identical wherever the user opens it.
export { ScoreInfoDialog } from '../library/ScoreInfoDialog.jsx'
