// CONTRACT (implemented by the projects module):
// <AddToProjectDialog open onClose scoreIds={string[]} />
// Lets the user pick one or more projects (or create a new one) and adds the
// given scores to them via addScoresToProject. Shows a toast on success.
export function AddToProjectDialog() {
  return null
}
